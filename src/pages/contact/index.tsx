import React from 'react';

export default function Contact() {
  return (
    <div>
      <h1>Ini adalah halaman Contact</h1>
      <h1>BLOG</h1>
      <div>
        {/* {data.map((datas: data, index) => (
          <div key={index} className="p-3 border rounded-lg mb-3">
            <h3> {datas.name} </h3>
            <p>{datas.price}</p>
            <small>{datas.size}</small>
          </div>
        ))} */}
      </div>
    </div>
  );
}
